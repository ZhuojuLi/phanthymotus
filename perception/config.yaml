mcp_port: 15720

plugins:
  asr:
    enabled: false
    mode: offline
    model_dir: /models/sherpa-onnx/asr
    hw_provider: cpu
    num_threads: 2
    language: zh-CN
    vad:
      model: sherpa_onnx    # sherpa_onnx (ONNX, lightweight) | silero (PyTorch) | webrtc
      threshold: 0.5
      silence_ms: 400
    kws:
      enabled: true
      model_dir: /models/sherpa-onnx/kws
      keywords:
        - "x iǎo f àn x iǎo f àn @小范小范"

  tts:
    enabled: false
    model_dir: /models/sherpa-onnx/tts
    speaker_id: 0
    speed: 1.0

  htmsg:
    enabled: false
    namespace: ""       # auto-detect from hostname if empty
    keyframe:
      dist_thresh: 1.0    # meters — insert keyframe when moved this far
      angle_thresh: 0.35  # radians (~20°) — or rotated this much
    semantic:
      model: "mobileclip"
      sam_model: "sam2_tiny"
      confidence_decay: 0.95
    graph:
      db_path: "/opt/phanthy-motus/data/htmsg"
      max_nodes: 500

  vop:
    enabled: false
    namespace: ""       # auto-detect from hostname if empty
    model: "yolov8s-worldv2"
    confidence: 0.3
    classes: []         # empty = default broad set
    fps: 5

  ocr:
    enabled: true
    provider: rapidocr
    backend: mnn
    fallback_backend: ""
    url: ""
    key: ""
    model: ""
    model_dir: /models/ocr/ppocrv6-small-mnn
    fallback_model_dir: /models/ocr/ppocrv6-small-ort
    language: zh
    use_angle_cls: false
    num_threads: 1
    # 评测环境配置（Jetson Orin 8G，10 容器并发）：960 / unclip1.2 / rec_min0.3。
    #   1600 配置下 det 推理内存峰值 ×10 容器超出 8G，容器被 OOM kill（退出码137）。
    #   960 像素量仅 1600 的 36%，内存与耗时大幅下降。v2 精度最优值为
    #   1600/unclip0.7/rec_min0.9（F1 0.6847），内存充裕环境可切回。
    max_side_len: 960
    det_unclip_ratio: 1.2
    det_thresh: 0.3
    det_box_thresh: 0.5
    rec_min_score: 0.3          # 回滚点配套值；v2 最优为 0.9（砍FP +8.4pp）
    min_interval_ms: 0
    large_image_strategy:
      # 关闭分块：tile 跨边界切割产生无法去重的碎片框, predict_T 膨胀 77%, F1 崩塌
      enabled: false
      trigger_side: 2400
      decode_side: 3200
      decode_hard_limit: 4096
      tile_size: 1280
      overlap: 128
      max_tiles: 12
      global_pass: true
      dedup_iou: 0.5
      dedup_text_similarity: 0.8
